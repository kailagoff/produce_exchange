<h2>All Users</h2>

<% if @users.any?() %>
  <ul>
    <% @users.each do |user| %>
    <li><a href="/users/<%= user.id() %>"><%= user.name() %></a>
    <% end %>
  </ul>

<% else %>
  <p>There are no users yet!</p>
<% end %>
