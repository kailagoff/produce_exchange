<h2>All Accounts</h2>

<% if @users.any?() %>
  <ul>
    <% @users.each do |user| %>
    <li><a href="/accounts/<%= user.id() %>"><%= user.name() %></a>
    <% end %>
  </ul>

<% else %>
  <p>There are no users yet!</p>
<% end %>
